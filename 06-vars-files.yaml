- name: print the variables 
  host: web
  vars:
    - vars.yaml:
  tasks:
  - name: print the course information 
    asible.built.debug:
      msg: "HI Iam learning {{COURSE}}, Duration is: {{DURATIONS}}, Timeings is: {{TIMEINGS}}, Trainer: {{TRAINER}}"
      
  